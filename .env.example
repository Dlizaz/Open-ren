# Runway API key (tạo tại dev.runwayml.com, KHÔNG phải tài khoản web thường)
# Chỉ bắt buộc nếu bạn dùng nguồn "Runway".
RUNWAYML_API_SECRET=

# Pixazo API key (lấy free tại pixazo.ai). Chỉ bắt buộc nếu bạn dùng nguồn
# "Pixazo (LTX)".
PIXAZO_KEY=

# URL công khai của chính web này (vd: https://open-ren.up.railway.app),
# KHÔNG có dấu / ở cuối. Dùng để tạo link ảnh public gửi cho Pixazo (Pixazo
# bắt buộc image_url phải là link internet thật, không nhận base64).
# ĐỂ TRỐNG là đủ trên Railway: server tự đọc biến hệ thống
# RAILWAY_PUBLIC_DOMAIN (Railway tự bơm sẵn, tự cập nhật mỗi khi bạn đổi tên
# service hoặc gắn custom domain mới) - không cần bạn tự set biến này nữa.
# Chỉ điền tay nếu chạy sau CDN/proxy riêng có domain khác với domain
# Railway đang thấy.
PUBLIC_BASE_URL=

# Connection string MongoDB Atlas (Database > Connect > Drivers), dạng:
# mongodb+srv://user:password@cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=

# Tên database dùng để lưu trạng thái + video + ảnh (không bắt buộc, mặc định open_ren_queue)
MONGODB_DB_NAME=open_ren_queue

# Google Drive: dung OAuth2 (dang nhap bang chinh Gmail ca nhan cua ban),
# KHONG dung Service Account nua (Service Account bi loi "khong co storage
# quota" voi Gmail thuong). Lay 3 gia tri nay bang cach chay 1 lan
# scripts/get-drive-refresh-token.mjs TREN MAY CA NHAN - xem huong dan chi
# tiet ngay dau file script do.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REFRESH_TOKEN=

# ID cua folder Google Drive muon luu video vao (lay tu URL folder) - folder
# nay phai nam trong CHINH DRIVE CUA BAN (tai khoan da dung o buoc tren)
GOOGLE_DRIVE_FOLDER_ID=

# Số video render song song cùng lúc
QUEUE_CONCURRENCY=2

# Railway sẽ tự set PORT, để trống khi deploy
PORT=3000
